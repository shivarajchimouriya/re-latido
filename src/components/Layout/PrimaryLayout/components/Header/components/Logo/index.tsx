import LogoIcon from "@/components/Icons/Logo";
import { Box } from "@chakra-ui/react";
import React from "react";

const Logo = () => {
  return (
    <Box>
      <LogoIcon />
    </Box>
  );
};

export default Logo;
